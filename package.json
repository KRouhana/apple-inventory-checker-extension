{
  "name": "apple-inventory-checker-extension",
  "version": "0.1.4",
  "private": true,
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "pnpm --dir apps/extension build:chrome",
    "package": "pnpm --dir apps/extension package:chrome",
    "check": "pnpm --dir packages/core check && pnpm --dir apps/extension check",
    "test": "pnpm --dir packages/core test && pnpm --dir apps/extension test",
    "catalog:check": "pnpm --dir packages/core build && node scripts/catalog/validate-portable-catalog.mjs",
    "format:check": "prettier --check .",
    "ci": "pnpm catalog:check && pnpm format:check && pnpm check && pnpm test && pnpm package"
  },
  "devDependencies": {
    "@types/node": "24.13.4",
    "prettier": "3.6.2",
    "typescript": "5.9.3"
  }
}
